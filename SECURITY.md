# 安全策略

## 报告漏洞

请通过 GitHub 仓库的 **Security → Report a vulnerability** 私密报告安全问题，不要创建公开
Issue。报告应包含受影响版本、复现步骤、潜在影响和建议修复方式。

维护者确认问题前请勿公开细节。项目会尽快确认报告、评估影响，并在修复可用后协调披露。

## 秘密与权限

- `.env` 永远不应提交；日志不得记录 token、API key 或完整 webhook。
- GitHub token 仅需公开仓库只读权限。
- 飞书与 LLM 密钥应使用部署平台的秘密管理能力，并定期轮换。
- Docker 容器默认以非 root 用户运行，不暴露网络端口。
